{
    "name": "mern-exercise-tracker-server",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "@hapi/joi": "^15.1.0",
        "bcrypt": "^3.0.6",
        "cors": "^2.8.5",
        "dotenv": "^8.0.0",
        "express": "^4.17.1",
        "jsonwebtoken": "^8.5.1",
        "mongoose": "^5.6.9"
    },
    "scrpts": {
        "start": "node server.js",
        "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
    },
    "engines": {
        "node": "12.2.0"
    }
}